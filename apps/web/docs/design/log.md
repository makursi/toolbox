# 待办与验证记录

> **管什么**：还没做完的事，以及历次浏览器验证的方法与证据。**和它一起读**：`apps/web/docs/design/checklist.md`（每次要过的检查）。
>
> **条目标注**：每条开头标 **所属**（工作项目：image-converter / 站点外壳 / 文档 / 通用）· **内容**（一句话）· **时间**（取自 commit 或执行日）。

1. **封面图**（所属：image-converter · 内容：封面资产与规则 · 时间：2026-09-17，#13）**：已完成** —— 首张封面已入位（`apps/web/public/tools/image-converter/cover.jpg`）。以后每加一个 Tool 仍需一张 4:3 的封面；规格与投递位置见 `apps/web/docs/design/assets.md`。
2. **在浏览器里验证**（所属：image-converter · 内容：生产构建验证轮次 · 时间：2026-09-17 – 2026-09-20）**：**2026-09-17 已做三轮，只剩真机**。第一到八轮是 image-converter 的验证，第九轮起是站点级（量具、门禁、文档拆分），轮次级只标归属变化处。
   第一轮（审计）：浅色与深色、首页/工具页/404 都在 Chrome 里渲染并截图核对过；360 / 390 / 768 / 1024 / 1280 五个宽度都过了（无横向溢出，可点控件不小于 44px）；触控目标用 `elementFromPoint` 量过；禁用态与格式勾选的联动用**键盘**（空格）在生产构建里跑过（这一项只验了键盘，后来那个点击 bug 正是从这里漏过去的，见第五轮）；封面在有图/无图两条路径下都验过。`apps/web/docs/design/checklist.md` 里的机械项（破折号 0、无远程资源、层顺序）是对着生产构建产物跑的。
   第二轮（窄屏卡片、触屏拖拽区、配色开关、返回入口）：首页 5 个宽度 × 2 配色、工具页 4 种（触屏/鼠标 × 窄/宽）组合、开关的「点击 → 存储 → 刷新 → 再点」全链路，外加 `elementFromPoint` 量可点区域、浏览器无障碍树读按钮的可访问名字。
   第三轮（图标、以及本节剩下的四处缺口）用无头 Chrome 对着 `pnpm build && pnpm start` 跑：
   - **开关的可访问名字**（图标改完之后的遗留）：浅色下无障碍树读到「切换到深色」，深色下读到「切换到浅色」；Lighthouse 的 `button-name` 审计两个配色都满分。图标本身在这个尺寸下是 14px，底色拿到的是当前正文色 token。
   - **加文件 → 转换 → 下载，端到端**：64×48、左半透明的 PNG 进，五种目标格式各出一份；**25 组输入×输出组合全部能被 Chrome 自己解码回来**，尺寸全是 64×48；有透明通道的来源在 PNG/WebP/AVIF 里保持透明（`0,0,0,0`），在 JPEG/BMP 里落到纯白 255（当时是可以选的背景色，默认就是纯白，见第五轮）而不是黑。一次转换五个目标约 1.7s，全程零外发请求、零控制台报错。
   - **动效看过**：`animationstart` 收到 `reveal`，逐帧采样得到 `translateY(12px)` → 0、opacity 0 → 1，第二个区块延迟 `0.08s`；悬停阴影 `none` → `rgba(0,0,0,0.04) 0 2px 8px`，过渡 200ms。
   - **reduced-motion**：动画确实全部关掉了（`document.getAnimations()` 为空、`.reveal` 的 `animation-name` 是 `none`），也因此发现 `.lift` 的过渡漏在门控外，已按 `apps/web/docs/design/motion.md` 修好。
   - **Lighthouse 12**（移动端模拟，每个页面 × 配色各一次，单次不取平均）：首页性能 **0.91**（浅）/ **0.97**（深），工具页 **0.83**（浅）/ **0.84**（深）；**可访问性、最佳实践、SEO 三项全部 1.00**，`color-contrast` 与 `button-name` 在内。性能被 LCP 3.0–3.4s、TBT 200–330ms 拖着，两个数字都出自移动端模拟，也没有被设成过目标。
     第三轮的脚本是一次性的，没有进仓（要重复就得再写一遍，或者把浏览器检查做成仓里的东西，那是一个待决项）。顺便纠正一条旧判断：无头浏览器**能**把文件塞进 `input[type=file]`（走 CDP 的 `DOM.setFileInputFiles`），React 照常收到 change——之前说不能，是因为当时的工具没这个能力，不是浏览器不行。
     第四轮（工具自己的 QA 清单，就是 #19 做的）：**全部条目通过**，方法同上——无头 Chrome 对着生产构建，用 CDP 的 `DOM.setFileInputFiles` 把文件送进文件选择器。
   - **质量改变大小**：512×512 的真噪声图，质量 1 → 100：JPEG 1227 → 765220 字节，WebP 44188 → 291916，AVIF 398 → 454984。
   - **无损**：WebP 786512 ≥ 无损前的 291916，AVIF 1112256 ≥ 454984；无损打开时质量滑杆 `aria-disabled="true"`、`tabindex="-1"`。
   - **高级面板有效**：AVIF `speed: 0` 用 138.3s 得到 142403 字节，`speed: 10` 用 0.24s 得到 144411 字节（快约 580 倍、文件略大）；oxipng `level: 0` 用 322ms 得到 787072 字节，`level: 6` 用 1223ms 得到**同为** 787072 字节（不更大）。
   - **命名冲突**：`photo.png` 与 `photo.jpg` 同一批 → `photo.webp` 与 `photo-1.webp`。
   - **超限**：103 MB 的 BMP 被拒并给出「这个文件有 103 MB，上限是 100 MB。」，同一批里的另一个文件照常转换完成。
   - **坏文件**：只有 PNG 签名的文件单独失败、同批其余照常完成。这一轮当时报的是浏览器的英文原文，按 #20 改成自己写的中文句子（「这个文件解不开，可能已经损坏或者不完整。」），浏览器原文改去 console。
   - **取消**：在「已完成 0 / 1」时取消 → 结果与进度清空、文件仍在列表里、取消按钮消失。
   - **纯键盘**：在「选择文件」上按 Enter 确实打开了文件选择器（`Page.fileChooserOpened`），再按 Enter 转换、按 Enter 下载，文件落盘。
   - **大图不卡页面**：13.7 MB / 2000×2000 的 PNG 转 WebP 用 4.6s，期间 **Long Task 数为 0**、最长帧间隔 29ms、页面对每次都只花 1–2ms 就回了话。转换确实全在 Worker 里。
   - **一批 2 源 × 5 目标**：零 CSP 违规、零控制台报错与警告、零 page error、零外发请求。
   - **像素上限那条没有用真图验**：268 MP 要差不多 1 GB 内存才造得出来，它由 `limits.test.ts` 的单测覆盖，注释里也说了这是唯一能在各浏览器上一致的做法。
   - **#20 修好之后复验**：坏文件给出的是本项目写的中文句子（「这个文件解不开，可能已经损坏或者不完整。」），浏览器的原文进 console 而不是界面；取消时 console 一行都不打；正常一次转换不受影响。
   - **一条经验**：这类验证的坑（生产构建而不是 dev、图必须不可压缩、要等状态转换而不是等元素消失）现在统一写在 `AGENTS.md` 的 Gotchas 一节，本文件只记证据。
     **第五轮（格式行、文件列表、删掉输出设置之后，2026-09-17）**：22 项全部通过，同一个无头 Chrome 加 CDP 的手艺，对生产构建跑。这一轮的重点是**用鼠标而不是键盘**：一次真实的 `mousePressed` / `mouseReleased` 打在格式行上——行首的方块、文字、文字下方 20px（仍在行内）各一次，状态每次都变；WebP 默认开着，这次能把它关掉（上一轮那个 bug 的表现是「只有 WebP 能选」）。无损坏开关用真鼠标点击从关到开（`classNames={{ body }}` 把 44px 靶子挂在 `<label>` 上），清空按钮同样。可点区域不是量边框，而是从控件中心向外逐像素问 `elementFromPoint`，直到答案不再是这个控件：格式行 241×58、开关 217×48、清空 159×52、文件行 358×49。文件列表：送进两个文件后读到「已添加 2 张」，叉掉一个剩一个，清空后为 0。端到端：64×48、左半透明的 PNG 开 PNG + JPEG 两个目标，输出都是 64×48（缩放路径已不在），PNG 左半边仍是 `0,0,0,0`，JPEG 左半边是 `255,255,255,255`、不透光的右半边两边都是 255,0,0。零外发请求、零 console 报错与警告，360 / 390 / 768 / 1024 四个宽度的横向溢出都是 0。页面上剩下的步骤标题是「1. 添加图片 / 2. 转换为 / 3. 下载」。
     **第六轮（组件与 hook 分目录、卡片抽成组件之后，2026-09-17）**：这一轮是纯搬家，所以验收办法是“把前后两份指纹逐项对齐”，不是“看过一眼”。同一套无头 Chrome 与 CDP：先对**改造前**的生产构建抓一份指纹，改完再抓一份。指纹包含两个页面 × 5 个宽度（360 / 390 / 768 / 1024 / 1280）× 两套配色下的正文文本、链接列表、**带几何与 Mantine 类名的 DOM 大纲**、以及关键元素的计算样式。两次比较 **84 项全等**，所以“移动”是真的移动。功能面 21 项全过：格式行用真鼠标点、WebP 取消勾选、无损开关、两个文件入队与「已添加 2 张」、叉掉一个、PNG+JPEG+WebP 三份输出且尺寸都是 64×48、PNG 与 WebP 保持透明、JPEG 落到纯白、坏文件给出的正是 `core/admission.ts` 的那句、清空能收掉它、取消能清空进度与结果而文件仍在列表里、每个下载链接都是 `blob:` URL（那个共用 hook 的职责所在）、零外发请求、零 console 报错与警告。
     **第七轮（文件行的缩略图与格式，以及清空键改为两处同清之后，2026-09-18）**：33 项断言全部通过。脚本仍是一次性的（没有进仓），手艺同前：无头 Chrome 加 CDP 对着 `pnpm build && pnpm start` 的生产构建，`DOM.setFileInputFiles` 送文件，`Input.dispatchMouseEvent` 打真鼠标。这一轮的两件主角、以及四条量出来的事实（20 张 2000×1500 的缩略图 510–670ms 出齐、最长一次长任务 51ms；解码失败的行只剩文字、框里既无图也无边框；删一行不会让其他行重挂重解码；移除叉声明 44×44、实测可点 43×43，而 Mantine `Button` 上的伪元素被 `overflow: hidden` 裁成按钮自己的框）逐条记在 `apps/web/src/tools/image-converter/README.md` 的手工清单一段里。
     **第八轮（站点标记换成角色插画、格式行只剩勾选框之后，2026-09-18）**：54 项断言全部通过，仍是一次性脚本（没进仓）、同一套无头 Chrome 加 CDP 对生产构建（`pnpm build` 后 `next start -p 3111`，Chrome 153 headless=new）。三件事：
     - **`<head>` 与图标**：预渲染 HTML 里真的写进了三条链接——`icon1.png`（32×32）、`icon2.png`（128×128）、`apple-icon.png`（180×180）；三个文件在页面里都能解码成声明的尺寸，页头那张 `/brand/makursi.png`（128×128）是 `next/image` 优化后送出的。
     - **页头**：标记在 5 个宽度（360 / 390 / 768 / 1024 / 1280）× 两套配色下都是 28×28、页头总高 **61px**（上限 80px）、横向溢出 0；两套配色各截了一张图核对——深色下角色仍认得出，因为帽子虽深、脸与围巾是亮的。
     - **一次真转换**：64×48、左半透明的 PNG 进，五个目标全开，五份输出都出来、尺寸全是 64×48、不透光的右半边仍是红（PNG/BMP 精确 `255,0,0`，JPEG `254,0,0`、WebP `255,0,2`，有损的正常漂移）；PNG/WebP/AVIF 保持 `0,0,0,0`，JPEG/BMP 落到纯白——也就是删掉 `core/options.ts` 之后编码路径没断，质量确实取自 `formatSpecs`。格式行用真鼠标点（可点区域 886×44，四个新勾上的都点着了），页面上 `[role=slider]`、Switch、`高级选项`、`质量：` 各 0 个，零外发请求、零 console 报错与 CSP 违规。
     - **顺手量到的一条**：把输出读回来时我第一版用了 `fetch(blob:…)`，被 CSP 的 `connect-src 'self'` 拦下——那是策略在正常工作；改成读 `URL.createObjectURL` 存下来的 Blob 对象即可（脚本里注了这一点）。
       **第九轮（`Button` 裁掉伪元素那件事修掉之后，2026-09-20）**：这一轮的量具本身进了仓——`apps/web/scripts/touch-targets.mjs`（`pnpm --filter @toolbox/web touch-targets`），因为要验的就是「可点区域」这个只看得见渲染结果的性质。**改前**对未修改的生产构建跑：24 条失败，正好是两个 `Button` × 4 个宽度 × 2 套配色——配色开关声明 `44px × 44px`、实测 31×25，清空实测 45×27（同一份探针还量到返回首页 73×45、文件行的移除叉 43×45 本来就在线上）。**改后**同一脚本：配色开关 43×43、清空 45×45、返回首页 73×45、移除叉 43×45，且四处**绘制尺寸一个都没变**（32×26 / 46×26 / 74×20 / 28×28），四个宽度上的横向溢出都是 0，每页的控件数也过了一遍（首页 1 个，工具页 **4** 个：页头开关在每条路由上，再加返回首页、清空、行内移除叉）。守卫的行为单独实证过：在按钮上加 `data-loading`，计算出的 `overflow` 从 `visible` 回 `hidden`，去掉再回 `visible`。`ui-fingerprint.mjs` 的前后两份指纹也逐项对过：84 项全等，所以「只动了这两处」不是感觉。
       **第十轮（把 App 自己的文档搬进 App、并写清两层浏览器检查的归属之后，2026-09-20）**：这一轮同样是纯搬家——`docs/design.md` 与 `docs/adding-a-tool.md` 移进 `apps/web/docs/`，47 处引用、共 25 个文件跟着改（`apps/web/src` 下改到的全是注释行），界面一行没动，所以验收办法照第六轮：先对 `main` 的生产构建抓一份指纹，切回分支再抓一份，`ui-fingerprint.mjs` 比对 **82 项全等**。这个「全等」不是空转，有两件事证明：两份构建的 `BUILD_ID` 不同（`RPh6o6TVXk9Tg4ahVVxp_` → `CZ6f45u2P3kULwuK2sL1V`），先后服务的确实是两份不同的构建；再把一份指纹里 `360-light` 的 `padding-top` 从 `20px` 改成 `21px` 当对照，比较器立刻报 1 处不同并以 exit 1 退出。这一轮没有新的手工检查项，因为它没有改界面。
       **第十一轮（设计文档从一份文件变成一套文档，2026-09-20）**：`apps/web/docs/design.md`（260 行）拆成入口加九个模块（`apps/web/docs/design/`），48 行引用分布在 26 个文件（其中 47 处由脚本改写成路径，剩下那处是第十轮记录本身，按「原样搬」保留），另有 4 处只写「section 4」这类节号的注释一并改掉，决定记在 `docs/adr/0013-the-design-doc-is-a-doc-set.md`。内容是**搬家不是重写**：旧文件除了 28 处有意改写的行（引用改路径、入口前言、原第十三节改写成路由表），其余每一行都原样落在某个模块里，这一点是拿「旧文件的每一行在新集里逐行查在不在」核对的。验收照第六轮：先对 `main` 的生产构建抓一份指纹，改完再抓一份，`ui-fingerprint.mjs` 比对 **82 项全等**（两个页面 × 5 宽度 × 2 配色 × 文本/链接/大纲/计算样式）。这个「全等」同样不是空转：改后的指纹取自一次缓存未命中的真构建（`BUILD_ID` `OYNCDZQcklx5PMGmQlShm`，而抓「改前」那份时服务的是缓存里 `main` 的构建），并且把指纹里一处 `padding-top` 改成 `21px` 当对照时，比较器立刻报 1 处不同、exit 1。这一轮没有新的手工检查项，因为它没有改界面。
       **仍未做的**：真机（手机）已在 2026-09-17 验过（见 #15），所以本节不再有待验项。
       **一条经验**：交互要对着 `pnpm build && pnpm start` 验，不是 dev。dev server 在 HMR socket 连上之前不 hydrate，而它的源守卫会把不带 `Origin` 的客户端放进来、把浏览器拦在外面——页面看着是好的，实际每一下点击都没反应。
3. **要不要把浏览器检查做成仓里的东西**（所属：站点级 · 内容：量具与门禁分层决策 · 时间：2026-09-17 两次否决 – 2026-09-20 门禁落地，ADR-0012）**：**上面那五项（可访问名字、四种格式的端到端、动效、reduced-motion、Lighthouse）现在属于「一次性的证据」，靠一段没进仓的脚本得到。它这一轮抓到了一个真实缺陷（`.lift` 漏在 reduced-motion 门控外，审计不会报，只有真的去模拟 reduce 才看得出来）。值不值得因此引入一个浏览器测试依赖（比如 Playwright）、并给它一个 CI 作业，是一个要单独决定的事，别顺手加。**2026-09-17（第五轮）又否了一次**：那一轮修的是只有鼠标才发现得了的缺陷，是最像引入浏览器的理由，选择仍然是把陷阱写进注释、`AGENTS.md` 与手工清单（见第五轮），所以这条继续开着。
   **2026-09-17 已按最小口径落地一部分**：`apps/web/scripts/ui-fingerprint.mjs`（`pnpm --filter @toolbox/web fingerprint capture|compare`）进了仓，只做两件事——把两个页面 × 5 个宽度 × 2 套配色抓成一份指纹（正文、链接、带几何与 Mantine 类名的 DOM 大纲、关键计算样式），再比对两份。它堵的是这类改动唯一说不清的问题：「只是搬家，界面一个字没变」。仍然**不进 CI**（CI 里没有浏览器），也**不引入 Playwright**：指针、键盘、端到端转换依旧是一次性脚本，结论记在本节。要扩这个脚本之前先问一句——只用 CDP 与 `node:` 内置模块能不能做到？能，就不加依赖。
   **2026-09-20 又进了一个，同一个口径**：`apps/web/scripts/touch-targets.mjs`（`pnpm --filter @toolbox/web touch-targets`）。issue #29 要的正是一个只看得见渲染结果的量（声明在那儿、命中区域不在），而它又不是一次性脚本能收尾的事——下一改就可能再有人给某个自带 `overflow: hidden` 的 Mantine 控件挂上 `.touch-target`。做法是从每个控件的中心逐像素向外问 `elementFromPoint`，**两侧都要答**才算数（所以「靶子长大盖住邻居」也会读成不达标），360 / 390 / 768 / 1024 × 两套配色，并且数一遍每页该有几个控件——一个什么都没量到的探针会「通过」，那是这个检查唯一能骗人的地方。它仍然不进 CI、不加依赖：CDP 加 `node:` 内置模块就够了，「不加依赖行不行」这个问句这次的答案同样是可以。两个脚本共用的那条 CDP 连接抽在 `apps/web/scripts/cdp.mjs`。
   **2026-09-20 当天又定了一次，这次是反着的**：上面那条规矩只继续管**量具**，浏览器这一层另外立了一个**门禁**——`apps/web/e2e/` 加 `@playwright/test`（`pnpm e2e`；Turborepo 里 `e2e` 依赖 `build`，CI 里是独立一步，跑的是 `pnpm build && pnpm start` 的生产构建）。理由不是「CDP 做不到」——恰恰相反，第一版要买的三件事（真鼠标点格式行、一次转换出五种格式并解码回来、零外发请求与零 console 报错）在上面九轮里全都用 CDP 做过，所以**「能力」那一列是空的**；缺的是**形式**：那九轮的脚本是一次性的、没进仓（原文：「要重复就得再写一遍」），而 `touch-targets.mjs` 已经证明了另一半——一个没人记得跑的检查等于不存在。于是分界定成一句话：**量具管几何与计算样式（本地、同一台机器测两次），门禁管行为（每次改动、跑在 CI、红了挡 PR）**。落到文件上是：几何类的断言（44px、有没有移动）留在 `scripts/`，行为类的断言（点下去有没有反应、文件进得去出得来）进 `e2e/`；两边都说得通时归 `scripts/`，因为几何要的是像素而不是预期值。代价照实记：机器上多一份 Playwright 钉死的 Chrome for Testing（153，正好和量具量过的那个大版本同一档），`pnpm install` 不会下它、要单独 `playwright install chromium`，CI 多一步加一个按 Playwright 版本做 key 的缓存。**红一次的证据**：把 issue #29 那个 bug（`.touch-target` 挂回格式行的根 `<div>`）注回生产构建，10 个格式行用例（5 格式 × 2 宽度）全红，拿掉即全绿——守卫证明过自己能失败。决定记在 `docs/adr/0012-playwright-for-the-browser-gate.md`。
4. **中文字体是否自托管**（所属：站点外壳 · 内容：字体策略与待定项 · 时间：2026-09-14 成文，设计指南 #11）**：**目前用系统字体（见 `apps/web/docs/design/typography.md`）。如果在意跨平台观感一致，再换成 `next/font` 自托管。
5. **网格**（所属：站点外壳 · 内容：网格触发条件 · 时间：2026-09-20，#39）**：**触发条件只有一个，写在 `apps/web/docs/design/layout.md`（第二个 Tool 到来时）。这里只留改的时候要守的形状规则——有几个内容就几个格子，不要为了凑行留空格子。
6. **工具页**（所属：image-converter · 内容：页面结构与输出设置定稿 · 时间：2026-09-18，#30）**：这条记的是 image-converter 自己的页面结构**——三个步骤保持可见（添加图片 / 转换为 / 下载）、页面上没有任何东西折叠、没有输出设置。它是**工具级规则**，正身现在在 `apps/web/src/tools/image-converter/rules.md`；站点级外壳（返回首页、标题、描述、metadata）由 `src/components/tool-page/` 提供，见本站 `design.md` 的路由表。输出设置不要再加回来，理由见 `docs/adr/0010-no-output-settings.md`（含它的作用域声明与更新段）。
